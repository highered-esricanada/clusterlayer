{
  "name": "ClusterLayer",
  "version": "0.0.1",
  "description": "A Point Cluster Layer for ArcGIS API for JavaScript 4.x",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "grunt build"
  },
  "license": "Apache-2.0",
  "author": "Education and Research, Esri Canada",
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.1.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-uglify": "^3.3.0",
    "supercluster": "^3.0.2"
  }
}
